\documentclass{standalone}

\usepackage{pgfplots}
\pgfplotsset{compat=1.16}

\begin{document}

% start
\begin{tikzpicture}
  \begin{axis} [
      enlargelimits=false,
      axis on top=true]
    \addplot graphics
             [xmin=-3,xmax=3,
               ymin=-3,ymax=3]
             {exportedfig_t.png};
    \addplot [
        contour prepared = {
          draw color = black,
          labels = false
        },
      contour prepared format=matlab,
      dashed
    ]
    table {exportcontour.txt};
  \end{axis}
\end{tikzpicture}
% end

\end{document}
